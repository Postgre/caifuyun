package com.zepan.android.widget;import android.content.Context;import android.util.AttributeSet;import android.widget.TextView;/** * 当控件点击后，alpha值发生变化 * */public class AlphaTextView extends TextView {	// 点击后改变的alpha值	private int clickAlpha;	public AlphaTextView(Context context, AttributeSet attrs) {		super(context, attrs);	}	public void setClickAlpha(int alpha){		this.clickAlpha = alpha;	}		@Override	public void setPressed(boolean pressed) {		super.setPressed(pressed);		if (pressed) {			// Drawable background = getBackground();			// if(background != null){			// background.setAlpha(100);			// }			// 改变字体透明度			this.setTextColor(getTextColors().withAlpha(clickAlpha));		}else{			this.setTextColor(getTextColors().withAlpha(255));		}	}}