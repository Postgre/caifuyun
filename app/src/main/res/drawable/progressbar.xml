<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dip" />

            <gradient
                android:angle="0"
                android:centerColor="#ebeded"
                android:centerY="0.75"
                android:endColor="#ebeded"
                android:startColor="#ebeded" />
        </shape>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="10dip" />

                <gradient
                    android:angle="0"
                    android:centerColor="@color/bg_home"
                    android:centerY="0.75"
                    android:endColor="@color/bg_home"
                    android:startColor="@color/bg_home" />
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="10dip" />

                <gradient
                    android:angle="0"
                    android:endColor="@color/bg_home"
                    android:startColor="@color/bg_home" />
            </shape>
        </clip>
    </item>

</layer-list>